# This file ensures that the app directory is treated as a Python package
# and allows importing from it. It also applies any necessary compatibility patches.

# Apply compatibility patches for newer Python versions
"""from app.compat_patches import apply_compatibility_patches
apply_compatibility_patches()"""